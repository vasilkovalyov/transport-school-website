import { Container } from '@/components/theme';

function SectionServerError() {
  return (
    <section className="section-server-error">
      <Container className="section-server-error__container">
        <h4 className="ta-c">Something wend wrong with server</h4>
      </Container>
    </section>
  );
}

export default SectionServerError;
